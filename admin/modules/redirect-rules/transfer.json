{
  "ui": { "type": "section", "icon": "tabler-file-spreadsheet", "class": "pa-5" },
  "name": "Import & export",
  "subtitle": "Move rules in and out as text. Everything on this screen is copy-and-paste — there is nothing to upload and nothing to download.",
  "elements": [
    {
      "ui": { "type": "row", "dense": false },
      "elements": [
        {
          "ui": { "type": "field", "cols": 12 },
          "key": "import_csv",
          "type": "textarea",
          "label": "Paste your rules here",
          "placeholder": "match_type,from_path,query_match,to_path,code,priority,status,starts_at,ends_at\nexact,old-page,,new-page,301,0,active,,\nprefix,blog,,news/$1,301,0,active,,",
          "rows": 12,
          "hint": "Open the file you exported from your old site in a spreadsheet or a text editor, select everything, copy it, and paste it into this box. There is no file picker on this screen — the text is the upload.",
          "rules": ["nullable", "string"]
        },
        {
          "ui": { "type": "field", "cols": 12, "valueClass": "text-body-2" },
          "key": "import_help",
          "type": "display",
          "label": "What each column means"
        }
      ]
    },
    {
      "ui": { "type": "row", "dense": true },
      "elements": [
        {
          "ui": { "type": "button", "color": "primary", "icon": "tabler-upload", "size": "large" },
          "action": "save",
          "label": "Import rules"
        }
      ]
    },
    {
      "ui": { "type": "row", "dense": false },
      "elements": [
        {
          "ui": { "type": "field", "cols": 12 },
          "key": "export_csv",
          "type": "textarea",
          "label": "Copy your rules from here",
          "rows": 12,
          "readonly": true,
          "hint": "Every rule you have, in the same format the box above accepts. Click inside, select all, and copy. To keep it as a file, paste it into a text editor and save it with a .csv ending — a spreadsheet will then open it as columns. Copy this before a big import, and you have a way back."
        }
      ]
    }
  ]
}
